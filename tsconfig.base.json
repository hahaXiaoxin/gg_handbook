{
    "compilerOptions": {
      // 编译目标
      "target": "ES2020",        // 将 TypeScript 代码编译为 ES2020 标准的 JavaScript
      
      // 模块相关
      "module": "ESNext",        // 使用最新的 ES 模块系统
      "moduleResolution": "bundler", // 模块解析策略，bundler 适用于 Vite/Webpack 等打包工具
      
      // 严格检查
      "strict": true,            // 启用所有严格的类型检查选项
      
      // 模块兼容性
      "esModuleInterop": true,   // 允许使用 import 导入 CommonJS 模块
      
      // 其他检查选项
      "skipLibCheck": true,      // 跳过对声明文件（.d.ts）的类型检查，提高编译速度
      "forceConsistentCasingInFileNames": true,  // 强制文件名大小写一致性
      "resolveJsonModule": true, // 允许导入 JSON 文件
      "isolatedModules": true,   // 确保每个文件都可以单独编译
      
      // JSX 支持
      "jsx": "preserve",         // 保留 JSX 语法，不做转换（通常由其他工具如 Babel 处理）
      
      // 路径配置
      "baseUrl": ".",           // 设置基础路径为当前目录
    },
    "exclude": ["node_modules", "dist"] // 排除不需要编译的目录
  }